import styled from "@emotion/styled";

export const Button = styled.button`
  width: 100%;
  padding: 20px 0;
  border-radius: 0;
  background: none;
  
  &:hover {
    background-color: rgba(0, 0, 0, 0.1);
  }
`;
